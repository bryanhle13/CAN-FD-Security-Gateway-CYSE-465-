# Demo Scripts Overview

This directory contains instructions for running the demonstration scenarios for the CAN-FD
attack and defense system. All executable Python scripts and supporting files are located
in the `/src` directory. This folder serves as the documentation hub for how to execute
and reproduce the attack simulations and mitigation tests.

---

# Requirements

Before running any demos, install the project dependencies:

pip install -r ../requirements.txt
